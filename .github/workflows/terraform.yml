name: Terraform CI/CD

on:
  push:
    branches: [ main, master, develop ]
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/workflows/terraform.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/workflows/terraform.yml'

env:
  TF_VERSION: '1.6.0'
  AWS_REGION: 'ap-northeast-2'

jobs:
  terraform-validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        environment: [dev, staging, prod]
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Terraform Format Check
      run: terraform fmt -check -recursive

    - name: Terraform Init (Networking)
      run: terraform init -backend=false
      working-directory: environments/${{ matrix.environment }}/networking

    - name: Terraform Validate (Networking)
      run: terraform validate
      working-directory: environments/${{ matrix.environment }}/networking

  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: terraform-validate
    # PR에서만 실행 (develop 브랜치 push에서는 스킵)
    # GitHub Secrets에 AWS 자격 증명이 설정되어 있어야 함
    if: github.event_name == 'pull_request'
    
    strategy:
      matrix:
        environment: [dev, staging, prod]
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Configure AWS credentials
      id: aws-credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}
      continue-on-error: true

    - name: Terraform Init
      if: steps.aws-credentials.outcome == 'success'
      run: terraform init
      working-directory: environments/${{ matrix.environment }}/networking

    - name: Terraform Plan
      if: steps.aws-credentials.outcome == 'success'
      run: terraform plan -var-file="terraform.tfvars" -out=tfplan
      working-directory: environments/${{ matrix.environment }}/networking

    - name: Upload Plan
      if: steps.aws-credentials.outcome == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: terraform-plan-${{ matrix.environment }}
        path: environments/${{ matrix.environment }}/networking/tfplan

    - name: Skip message if no credentials
      if: steps.aws-credentials.outcome == 'failure'
      run: |
        echo "⚠️ AWS credentials not available. Skipping terraform plan."
        echo "To enable terraform plan in PRs, configure GitHub Secrets:"
        echo "  - AWS_ACCESS_KEY_ID"
        echo "  - AWS_SECRET_ACCESS_KEY"
        echo "See docs/GITHUB_SECRETS_SETUP.md for details."

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    strategy:
      matrix:
        environment: [dev, staging, prod]
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Download Plan
      uses: actions/download-artifact@v4
      with:
        name: terraform-plan-${{ matrix.environment }}
        path: environments/${{ matrix.environment }}

    - name: Terraform Init
      run: terraform init
      working-directory: environments/${{ matrix.environment }}

    - name: Terraform Apply
      run: terraform apply -auto-approve tfplan
      working-directory: environments/${{ matrix.environment }}

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: terraform-validate
    # PR에서는 CodeQL 업로드 권한이 없으므로 push 이벤트에서만 실행 (main/master 브랜치)
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v4
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'
